# 批次 B 独立复验 R9

2026-09-15。**R8金额污染反例修复有效；批次B仍为NO-GO，剩余一项P1：未识别期间表头下fallback仍猜列通过。** 保持暂停点二，不进入C。

本次独立使用新目录 `outputs/batch_b_independent_review_r9_20260915/`，未写入旧审查证据或实施方修复证据目录。

## 已确认

`src/engine/budget_rules.py` SHA-256：`d86f17e768a3de322d7a1baac940829f0ec3943c5e90df3176387982a4df4188`，与用户报告前缀一致。

当前表头分析排除含数字单元格的数据行，年份要求“年”字语义锚定。本轮重跑R8的上年金额1000与2050对照，两者均正确检出本年100≠90、差额10。之前累计探针的缺输入、缺列partial findings、单位和精度保护保持预期。

## P1：无“年”字表头不是fail-closed，换序时仍跨年假平衡

位置：`_extract_t1_t4_strict_totals`，无`current_cols`时取首个非prior数值的fallback路径。

用户报告已明确提及 `2025 预算数` 表头不再参与年份推断，但认为该路径方向是fail-closed。独立实际BUD-101验证表明，这个前提不成立。

文档标题“2025年部门财务收支预算总表”，单位万元，输入表：

| 项目 | 2024 预算数 | 2025 预算数 | 项目 | 2024 预算数 | 2025 预算数 |
|---|---|---|---|---|---|
| 收入总计 | 100.00 | 100.00 | 支出总计 | 100.00 | 90.00 |

实际返回`[]`。再把本年收入100.00改为空白，也返回`[]`。程序在年度无法识别时把最左侧的上年100收入与上年100支出进行比较，因此本年差异和本年缺输入都消失。

严格对照：仅将四个期间表头的空格改为“年”字，所有数据和列序不变：

- 本年收入100、本年支出90：正确报差额10。
- 本年收入空白、本年支出90：正确deferred，收入总计缺失。

这不是要求必须支持某种罕见排版，而是要求**不支持或无法确认期间时不得宣称比较通过**。本轮“金额不能当年份”的修复应保留，不能通过放松到任意四位数字重新引入R8。

## 最小整改要求

两种实现均可，选择有可靠依据的一种：

1. 对“年度数字 + 预算数”等明确期间语义作可靠解析，排除数据行和文号，并绑定文档目标年度；或
2. 将无法判定期间的多金额列组明确标为未知，返回deferred。

无论采用哪种方式，fallback仅能用于可证明唯一的单金额列；“未知列没有被标prior”不能成为它是本年列的证据。检查共用T1/T4函数，补BUD-101与BUD-105的T4消费者测试：上年在前/后、本年正常/错误/空白/0。保留当前R8及所有已关闭反例。

## 独立门禁与证据

本轮独立门禁：完整pytest **1214 passed、1 skipped**（107.14秒）；Ruff `src api scripts tests`通过；mypy `src api`通过（104 source files）。原始日志均在R9目录。

- `probe.py`、`probe_results.json`、`probe.log`：累计探针与带/不带“年”字、完整/空白四个严格对照；真实规则无mock。
- `pytest.log`、`ruff.log`、`mypy.log`：本轮独立执行。
- `golden_replay.log`、`golden_eval.log`：新鲜legacy回放与评估。
- 回放：`outputs/golden_replay/DOC-20260905-001-legacy-20260915-135023-504-4c2155bc208741548245d5947f8454fb.json`。
- 评估：`outputs/golden_eval/DOC-20260905-001-eval-legacy-20260915-215040-a089ef91a4f14691a37b943e0dcb02f6.json`。

新鲜Golden：TP=3、FP=0、FN=0，hint3/3、证据面4/4，GATE-PASS。它不覆盖上述期间歧义边界，不能代替B任务卡验收。

本轮仅新增R9报告和R9证据，未修改业务源码、既有测试、Golden真值，未提交、推送、部署或进入C。
