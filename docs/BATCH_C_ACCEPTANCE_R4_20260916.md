# 批次 C 第四轮独立复验与验收

日期：2026-09-16。

**结论：GO，批次 C（C01～C03）在本轮固定清单与既定整改范围内通过独立验收。** R3 的两个 P1 和一个 P2 已闭环，暂停点三的独立审核条件已满足。后续可按已约定的依赖完整清单安排本地提交；本报告不授权 push、部署或 NAS/生产变更。本轮仅审核，没有执行提交。

## 本轮独立验证

| 上轮问题/验证项 | 结果 |
|---|---|
| R3-P1-1：宽容差吞掉金额变化 | Decimal 按 0.01 万元显示精度进行换算后等值比较。1997.23 与 123.45 均拒绝配对并产生未决记录 |
| 完整 42 份账本的金额变体 | 仅将内存副本中 1997.19 改为 1997.23，matched 从 245 降为 244、pending 从 0 升为 3，无法再假通过 |
| 换算路径多候选 | 两个等价候选不再选首项；最小反例 matched=0、pending=4，歧义显式保留 |
| R3-P1-2：证据替换/删除 | 完整账本中替换或删除 BUD-111 原文证据，均保留业务对象关联但降为待审；matched=245、pending=1 |
| 展示回填核验 | 当前 32 条表示差异满足修前证据与 message 一致；pipeline 的无证据 message 回填逻辑与该解释相符 |
| 严重度表示映射 | pipeline._norm_sev 明确把 manual_review 映射为 warn，与当前 V33-120 表示差异解释一致；其他实质变化由新增测试验证进入待审 |
| 实施方生成器回归套件 | 独立执行 **8 passed**，包含金额、字段集合、多候选、证据、严重度及真实账本不变量 |
| 实际账本重生成 | 在独立目录重生成，JSON 与提交 diff_review 完全一致 |
| 42 份结果连续性 | findings、summary、页数、PDF SHA 与首轮独立真实重跑逐份一致 |
| 当前核心与工具指纹 | manifest.key_file_fingerprints 全部匹配；嵌套 path/sha256 条目全部匹配 |
| R3-P2-1：归档 SHA | excluded_evidence_archive.diff_review_sha256 与实际账本一致，与主指纹表同源同值 |

反例中的 pending 数字是生成器记录条数（可同时包含配对失败与新增/消失待裁决记录），不是独立业务缺陷数。所有反例在内存副本上执行，未修改正式 C 交付物。

## C01～C03 验收判定

- **C01 通过：** 固定 42 份材料，修前 247、修后 253，245 条关联、2 条消失、8 条新增，数学守恒；14 条单位表示配对通过精度核验，32 条证据表示差异与 1 条严重度表示差异已登记。当前比较账本 pending_review=0。
- **C02 沿用已独立通过的验证：** 首轮全量 1240 passed、1 skipped，Ruff/Mypy、legacy Golden（TP=3、FP=0、FN=0）及 45 项探针对照通过。此次业务核心指纹和样本结果保持一致；本轮没有重复执行全量门禁或 42 份 PDF 解析，新增执行的是生成器 8 项测试、独立反例和账本重建，不能将历史门禁描述为本轮新跑。
- **C03 通过：** 拟提交分组、依赖、文件范围与排除项已提供，关键代码、新增测试和外部证据引用有指纹；两条消失告警的业务判断与当前引擎 deferred 状态已分开登记，旧重放逻辑不再覆盖新比较账本。

## 必须保留的边界

1. **比较账本 pending=0 不等于规则执行全部完成。** 当前仍有 275 条 insufficient_data：真实材料 262 条、负例 13 条。这些未完成状态继续保留，不得改写成通过或“全系统零漏报”。
2. 规资局 V33-220 仍未完成项目支出取数，曹杨 BUD-107 仍未识别 T4 单位；旧告警的人工业务判断不等于引擎功能已完成。曹杨跨表单位解释是人工依据，不能冒充 T4 本页显式标注。
3. 本次放行限于冻结材料、既定整改契约与证据包。未对任意 PDF、每条存量 finding 的业务真值、生产性能或 NAS 环境作全面保证；真实输出数量不等于已独立确认的业务缺陷总量。
4. 后续若源码、材料或生成器继续改变，应更新指纹及受影响证据。当前生成器是本批证据工具，本验收不等于对任意新格式材料的通用匹配能力作保证。

## 证据与后续动作

本轮独立证据目录：`outputs/batch_c_independent_review_r4_20260916/`。

- `review.py` / `review.json` / `review.log`：完整账本与最小构造反例、指纹和结果连续性核验。
- `regenerated_diff.json`：独立重生成账本。
- `pytest_generator.log`：8 项生成器测试结果。

保留此前各轮独立报告和原始证据。本报告作为 C 阶段最终放行依据，可纳入最终文档提交组；本轮没有改动业务代码、Golden、原始 PDF，没有 commit、push、部署或访问 NAS。
