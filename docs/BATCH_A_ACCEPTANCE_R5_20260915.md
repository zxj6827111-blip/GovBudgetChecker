# 批次 A 最终复核与放行（2026-09-15）

**结论：批次 A（A01～A06）通过独立验收，GO，可以按既定任务包进入批次 B。** 本结论仅放行下一实施批次，不代表六组业务缺陷已经修复，也不代表系统整体生产就绪。本次未启动 B/C。

## 本轮独立核验结果

| 核验项 | 结果 |
|---|---|
| 生成器内存重生成与交付 JSON 比对 | 74 条规则记录完全一致 |
| apply 返回出口映射 | 未分类返回出口 0，上轮 7 项已逐一登记 |
| 同一路径分类冲突 | 0，原 53 处问题未复发 |
| BUD-105/107 指定必要数值守卫 | 漏记 0 |
| BUD-105 虚构 T1↔T3 分支 | 已删除，保持实际三个表对 |
| V33-246 国内接待已披露出口 | normal/pass，migration_card=none |
| 未识别条件兜底变体 | 新构造 doc.custom_condition 提前返回，明确记录 undetermined/pending_review，不静默丢失 |
| 当前源码及测试指纹 | manifest 中 8 个文件 SHA 均匹配，业务 diff 与上一轮一致 |
| 底账统计 | 74 条规则、214 条记录；16 budget/52 final/6 common |

214 是当前底账的分支记录数，不是业务必查义务总数或系统覆盖率。该底账作为后续实施清单使用，B 阶段仍必须依据实际代码和测试裁决业务语义，不能仅以条目计数证明修复完整。

上轮遗漏的七项已具体复核：BUD-003 年份未识别、BUD-109/V33-227 说明提取缺失、V33-240/241 列布局歧义均登记待迁移；V33-115 登记结构化合法委派；CMM-003 登记现有启用阈值及不适用口径。V33-246 不再把已披露情况降为未完成。

## 既有验收继续有效

本轮只改底账/生成器及交付说明，没有业务源码或测试变化，因此没有重复运行全量 pytest、静态检查和 Golden。保留第三轮已独立验证的：

- 当前工作树：1159 passed、1 skipped；Ruff 和 mypy（189 files）通过。
- 新鲜 legacy Golden：TP=3、FP=0、FN=0，GATE-PASS。
- 三入口 finding/未完成共存、状态优先级、同批有效/无效 Issue 转换与双原因保留。
- 实际 spawn 进程传输与项目文件存取边界后的严格 review_required/incomplete 判定。
- pre-A clean HEAD 的隔离目录和完整基线日志（1145 passed、1 skipped），与 post-A 分账。
- 可恢复 bundle/源码/材料备份核验、8 份决算与 33 份预算的输入冻结。

上述范围与限制沿用各轮报告：没有完成全部真实样本的业务真值裁决、前端/数据库端到端验收或生产部署验收。

## 下一批边界

允许按 `docs/GEMINI_IMPLEMENTATION_PACKET_V4_20260914.md` 执行 B01～B06。保留冻结输入、pre-A/post-A 证据与现有兼容契约；六组缺陷必须通过实际规则的正常、错误、缺输入、部分完成用例证明。遇到未决业务语义，记录依据，不猜测。

批次 B 完成后再次暂停独立验收，不自动进入 C。不修改 Golden 真值、不放宽门禁、不操作生产数据、不 push 或部署。A 放行不替代计划规定的最终提交/发布验收。

## 本轮证据

- `outputs/batch_a_independent_review_r5_20260915/check_inventory.py` 与 `evidence.json`：独立出口映射、冲突、生成一致性、指纹核验。
- 同目录 `fallback_probe.json`：未知条件显式待裁决的生成器变体结果。

本次仅新增最终验收文档和本轮隔离证据，未修改业务实现或底账，未提交、推送或启动 B/C。R1～R4 的历史 NO-GO 报告保留；其批次 A 阻塞状态由本报告关闭。
