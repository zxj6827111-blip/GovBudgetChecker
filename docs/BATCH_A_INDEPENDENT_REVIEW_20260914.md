# 批次 A 独立验收（2026-09-14）

**结论：NO-GO，A01～A06 尚不能整体通过；暂停 B/C。** 三入口的基本 partial finding 保留已实现，当前全量测试及静态检查通过，但备份、输入冻结、迁移分类及部分状态契约存在实质缺口。不是要求推倒重写，应在批次 A 内补齐下列问题后复验。

## 依据与已核实事实

- 已阅读任务“核实审校系统结论与上线缺口”（01a09eb4-69eb-7683-bcbd-0c37c2ce0d53），用户本次粘贴的 Gemini 完成报告，以及本地任务包 `docs/GEMINI_IMPLEMENTATION_PACKET_V4_20260914.md`。本次按任务包 A01～A06/C1 验收；报告称 v5 不改变已冻结的要求。
- HEAD 为 `63735c04a2e489dae57ab02cda9c469e6608eec7`；相对参考提交 `6438029` 的 Git tree 差异为空。采用新 HEAD 作为基线合理，无须回退。
- 已跟踪修改为报告所列 5 个文件，新增两份测试；manifest 中 7 个源码/测试 SHA 与当前文件匹配。未发现批次 B 规则业务代码修改。
- 独立执行 `python -m pytest -q`：**1155 passed、1 skipped、23 warnings，69.26 秒，退出码 0**。TEMP/TMP 指向仓库内 tmp；警告包含弃用提示和 pytest cache 写权限问题。
- 独立执行 `python -m ruff check .`、`python -m mypy api src tests`：退出码均为 0。mypy 有 untyped function 提示，不等于检查了所有未注解函数体。
- 独立无 resume 回放 corpus PDF 并评估：**TP=3、FP=0、FN=0，GATE-PASS**，hint 3/3、severity/page/evidence 指标均为 1.0。
- 5 个决算条目与 34 个预算条目的 PDF SHA 全部匹配；各自 SHA 唯一。SHA 唯一只证明文件不同，不证明是真实、可解析的业务材料。
- 抽查重跑《上海市普陀区人民政府石泉路街道办事处26单位.pdf》预算件：5 条 findings，逐条内容与交付基线一致。未逐份回放所有 39 个输入，也未做全部业务真值裁决。
- 只检查了本地状态，没有访问 NAS/云服务器、修改生产数据、提交或推送。无法由当前工作树独立证明实施期间从未推送。

## 必须返工的问题

### 1. [P1] A02 的“100% 恢复”仅证明 7 个材料副本相同，不能恢复源码和样本集合

备份路径 `C:\Users\zxj68\AppData\Local\Temp\govbudget_backup_20260914` 实际只有 8 个文件：清单中的 7 个文件和 manifest；还原目录只有相应 7 个文件。两处清单 SHA 确实全部匹配。

但没有 HEAD 的源码归档、Git bundle/对象基础或完整源码树；只有一个 corpus PDF，缺少冻结清单其余 4 个决算和 34 个预算 PDF，也没有它们的人工材料/基线输出。完整提交 SHA 是引用，不能替代可恢复的内容。

**整改与验收：** 保留现有副本，补充可恢复的 HEAD 基础及全部选定材料；明确真实空补丁；在另一个独立目录恢复 HEAD 和材料，按完整文件清单校验。恢复演练需包含源码文件，不能只复制材料目录相互比对。

### 2. [P1] A03 把空白测试 PDF 纳入“34 份独立真实预算材料”

清单首项 `uploads/02d630fd2f6933ff142e04b57517ced6/sample_budget_2025.pdf` 只有 **267 字节、1 页**。实查：文本为空、图片 0、绘图 0、内容流为空。其基线却以 budget 执行并产生 13 条 BUD-001。这不是已证明可用于真实预算材料验收的输入。

因此“34 份独立真实预算”不成立。排除这一项后至多剩 33 个候选，其他候选仍需材料类型和解析可用性核验。原计划的 53 是待核实数字，并非必须凑足的固定门槛；正确做法是核实来源与覆盖，不是仅因 34 小于 53 就宣告材料不足。

**整改与验收：** 按内容及来源识别测试件、空白件、扫描/无法解析件和真实预算件，保留去重与剔除原因；记录文种证据、页数、解析状态、配置和完整代码指纹。空白件可作为负面测试保留，但不得进入真实材料计数。

### 3. [P1] A03 决算回归集合替换了原约定样张，A04 修前日志不可追溯

前序审查的五份是 corpus、文旅、石泉路、宜川路、长风。交付清单变为 corpus、文旅、财政、规资、府办，未说明后三份的替换理由及原样张缺失状态。材料可以扩充，但不能静默替换已有缺陷回归输入；否则续表、零值及原人工对照等问题失去固定参照。

同时 `outputs/remediation_v5` 中只有 46 个 JSON 和一个 handoff.md，没有测试完整日志。A04 保存的是手写式摘要，Golden 命令为通配路径，未绑定具体 replay/eval 产物。当前复跑通过支持当前状态，不能倒推“修前已按指定范围完成全部检查”。

**整改与验收：** 找回并冻结原五份；若确实缺失，逐项列出缺失及影响，新增三份可作为补充。绑定准确脚本/命令、配置、输入 SHA、源码指纹和输出路径，保存完整日志与退出码。必要时在独立恢复的基线源码上补跑前测，明确是补建证据，不能回填成原执行记录。

### 4. [P1] A01 迁移底账并未逐分支裁决，且将仍有静默缺数据的规则标成稳定

JSON 实有 14 条决算规则，不是报告所写的 9 条；多处仅记录重复的 `return issues` 字样，没有行号、分支条件和四态判定。另有 38 条已注册规则包含 return 路径但未进入该底账；这不代表它们全部有错，但缺少逐项排除依据，不能声称完成全量分类。

更直接的反例：底账把 V33-101/102/106/107/108 标为“已在先前提交重构/保持稳定”。独立用缺表文档执行这五条，全部返回 `[]`，没有抛缺输入信号。例如 `src/engine/rules_v33.py:1449` 无锚点就返回 issues，1452 无表亦返回；这正是待分类的静默缺输入场景。

**整改与验收：** 每个相关分支记录位置、输入前提、当前状态、目标状态、业务理由和迁移卡号。不能因为此前改过合计或多栏位就豁免缺数据状态修复。A 阶段先修正底账，业务规则迁移仍属于 B。

### 5. [P2] A05 子检查“不适用”覆盖了已经确认的失败

`src/engine/rule_outcome.py:57` 的优先级将 not_applicable=20 放在 fail=10 之上；RuleNotApplicable 现在允许携带 partial_issues，三入口直接沿用 signal.status。

独立反例：子检查 A 已确认金额不一致，子检查 B 不适用，抛 `RuleNotApplicable(..., partial_issues=[Issue(...)])`。pipeline 保留了 1 条问题，但摘要是 **not_applicable=1、fail=0、failed_rules=[]、unresolved_total=0**。已确认问题没丢，但整条规则的分类与 C1“局部不适用不能覆盖其他子检查 fail”冲突。

**整改与验收：** 明确并实现组合状态契约：一部分失败、其余不适用且全部完成，应为 fail；有未完成则按未完成优先级处理。三入口一致，并测试 fail+not_applicable、insufficient_data+not_applicable、parse_error+insufficient_data 等组合。同步更新 pipeline.py:88 附近仍声称“非 fail 一律不产出 finding”的旧文档。

### 6. [P2] A05/A06 部分测试没有覆盖报告所称的边界

- `tests/test_quality_gate_coexistence.py:258` 所谓跨进程测试在第 270 行直接调用 `_rule_worker(...)`，没有创建 Process；证明了实际 Pipe 的序列化传输，但没有证明跨进程启动、父子进程及生产调用路径。
- 其他测试使用真实 payload 和质量门函数，有价值；但 pickle/JSON 往返仍为手动组装，未覆盖任务最终结果的存储/读取。应按任务包补全实际结果边界，避免把函数级通过扩写成整条任务链通过。
- `tests/test_audit_coexistence.py:285` 转换失败场景只有一个坏 Issue；有效 Issue 在另一次调用验证。尚未覆盖**同一规则、同一批 partial_issues 同时包含有效和无效结果**的 T07。

**整改与验收：** 补真实 spawn/生产结果传输路径测试并断言 worker PID 不同；通过实际任务结果边界读取 finding、summary、review_required/incomplete。补同批有效+无效 Issue 和双原因保留断言。仍须保持测试隔离，不能连接业务库或调用真实 AI。

## 任务卡裁决及下一步

| 卡号 | 裁决 | 剩余门槛 |
|---|---|---|
| A01 | 不通过 | 完整分支分类与稳定项反例纠正 |
| A02 | 不通过 | 源码、选定材料的完整备份与真实恢复 |
| A03 | 不通过 | 空白测试件剔除、原决算样张恢复、配置/指纹冻结 |
| A04 | 当前复跑通过；原前测证据不足 | 可追溯的修前完整日志和具体 Golden 产物 |
| A05 | 部分通过 | 部分不适用状态聚合、同批转换失败及兼容文档 |
| A06 | 部分通过 | 真实进程/结果边界证据和准确的报告口径 |

建议把本报告交给实施代理：**仅修复上述批次 A 问题，完成后重新交付并暂停，不进入 B/C，不提交或 push。** 无须重写已通过的基本保留流程，不得用放宽测试或修改金标准解决问题。

## 独立证据

- `outputs/batch_a_independent_review_20260914/probes.py`：可重复运行的只读审查脚本。
- `outputs/batch_a_independent_review_20260914/probe_results.json`：空白 PDF、备份清点、SHA 核验、静默返回及 not_applicable 反例。
- `outputs/batch_a_independent_review_20260914/budget_replay.json`：预算抽样回放和逐条相等判断。
- `outputs/golden_replay/DOC-20260905-001-legacy-20260914-111428-536-5324f9cf22044d3fbefc50be88ee88e1.json`。
- `outputs/golden_eval/DOC-20260905-001-eval-legacy-20260914-191446-5eb01b9b597245229f6e58969730c41d.json`。

本次仅新增审查报告及隔离输出证据，未修业务源码，也未启动 B/C。测试通过证明现有断言成立，不能替代备份恢复、输入真实性和未覆盖契约的验收。
